import { FilterTimeZonesPipe } from './filter-time-zones.pipe';

describe('FilterTimeZonesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTimeZonesPipe();
    expect(pipe).toBeTruthy();
  });
});
